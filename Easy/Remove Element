class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int final = 0;
        for(int i = 0; i< nums.size(); i++){
            if(nums[i] != val){
                nums[final] = nums[i];
                final++;
            }
        }
        return final;
    }
};
